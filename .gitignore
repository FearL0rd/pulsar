target/
Cargo.lock
*.bin.tmp
.gitnexus

# gitnexus local code-intelligence (tooling, not for the public repo)
.gitnexus/
CLAUDE.md
.claude/
AGENTS.md
err*.log
tmp/

# agora hub agent id: per-machine runtime state, written by the agent on join
.agora-agent-id